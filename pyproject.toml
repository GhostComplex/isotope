[project]
name = "project-isotopes"
version = "0.0.1"
description = "A pluggable Python agent framework"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
isotopes-core = { workspace = true }
